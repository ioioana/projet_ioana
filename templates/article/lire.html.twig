{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>{{ article.titreArticle }}</h1>
    <div>

        <img src="{{ asset('build/images/')}}{{ article.photoArticle }}" alt="">


        <p>{{ article.contenuArticle }}</p>
    </div>

    {% if app.user %}
        <a href="{{ path('article_index') }}"><i class="far fa-arrow-alt-circle-left"></i> Retour à la liste</a>
    {% endif %}
{#    <a href="{{ path('article_index') }}"><i class="far fa-arrow-alt-circle-left"></i> Retour à la liste</a>#}

    {#    <a href="{{ path('article_edit', {'id': article.id}) }}">editer</a>#}

    {#    {{ include('article/_delete_form.html.twig') }}#}
{% endblock %}
