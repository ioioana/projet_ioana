{% extends 'base.html.twig' %}

{% block title %}Package{% endblock %}

{% block body %}
    <h1>Package</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
{#                {{ dump(package) }}#}
                <td>{{ package.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ package.codePackage }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ package.prixPackage }} €</td>
            </tr>
            <tr>
                <th>services</th>
                {% for service in theServices %}
{#                    <tr>#}
                        <th>{{ service.nomService }}</th>
{#                    </tr>#}
                {% endfor %}
            </tr>
        </tbody>
    </table>

    <a href="{{ path('package_index') }}"><i class="far fa-arrow-alt-circle-left"></i> Retour à la liste</a>

{#    <a href="{{ path('package_edit', {'id': package.id}) }}">edit</a>#}

{#    {{ include('package/_delete_form.html.twig') }}#}
{% endblock %}
